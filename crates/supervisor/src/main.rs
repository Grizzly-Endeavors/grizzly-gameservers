use std::process::ExitCode;

// Placeholder entrypoint. The process-supervision runner, Agones SDK heartbeat,
// and axum control server land in subsequent stages.
fn main() -> ExitCode {
    ExitCode::SUCCESS
}
