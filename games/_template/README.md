# games/_template

**Placeholder — no implementation yet.**

Skeleton for onboarding a new game. Copy this directory to `games/<game>/` and fill in the base template (image, default env, port shape, resources, persistence). Format is an open decision — see `docs/design/00-overview.md`.
